import { TodoListOrderingPipe } from './todo-list-ordering.pipe';

describe('TodoListOrderingPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoListOrderingPipe();
    expect(pipe).toBeTruthy();
  });
});
